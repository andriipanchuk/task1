apiVersion: v1
kind: Namespace
metadata:
  name: web
  
---

apiVersion: v1
kind: Pod
metadata:
  name: test
  namespace: test
spec:
  containers:
  - name: busybox
    image: busybox
    resources:
      limits:
        cpu: "200Mi"
        memory: "1Gi"
